// Loaded async by LazyMotion so the full animation runtime stays out of the
// entry chunk. domMax includes layout animations + drag support.
import { domMax } from "motion/react";
export default domMax;
